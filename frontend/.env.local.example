# Локальная разработка (npm run dev). Копируется скриптом local-setup в frontend/.env

VITE_API_BASE_URL=/api
VITE_APP_NAME=YanParser

# Прокси Vite → Docker API (не IP VPS)
VITE_API_PROXY_TARGET=http://localhost:8080
